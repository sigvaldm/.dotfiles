#!/bin/bash

# FIXME:

# for f in .dotfiles/.*
# do
#     [ "$f" == "."    ] && continue
#     [ "$f" == ".."   ] && continue
#     [ "$f" == ".git" ] && continue
#     ln -s --backup=existing ".dotfiles/$f" .
# done
